#!/bin/sh
':' //; exec "$(command -v nodejs || command -v node)" "$0" "$@"






const HortenServer = require('../index')
    , server = new HortenServer()
server.configureCLI()
.open()
.then( () => {

} )
